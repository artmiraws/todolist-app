# Desired dev image, updated by CI (.github/workflows/deploy-dev.yml). Argo CD reconciles it.
# Only the image digest lives here; the rest of the wiring (repository, database, ingress) is
# injected inline by OpenTofu so no account IDs, ARNs, or hostnames are committed. See ADR-012.
image:
  digest: "sha256:dad7f9d71462bfd55ae365e5d4391a9c0d708aca187704d4b8d38aefc7a68535"
